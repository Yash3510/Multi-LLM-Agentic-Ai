# Local-only Phase 3 accelerators. The application keeps safe fallbacks when these are absent.
numpy>=1.26
turbovec>=0.2
pypdf>=5.0
Pillow>=10.0
pytesseract>=0.3
pdf2image>=1.17
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6
pandas>=2.0
